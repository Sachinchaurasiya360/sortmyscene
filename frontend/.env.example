# Base URL the frontend sends API requests to.
# Dev: leave as /api (Vite proxies it to http://localhost:5000).
# Prod: full URL of the deployed backend, e.g. https://sortmyscene-three.vercel.app/api
VITE_API_BASE_URL=/api
